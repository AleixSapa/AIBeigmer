const API='/api';const titles={dashboard:'Dashboard',models:'Models',benchmarks:'Benchmarks',execute:'Executar',results:'Resultats',compare:'Comparar',ranking:'Rànquing',settings:'Configuració'};let categories=[],models=[],questions=[];const $=s=>document.querySelector(s);async function get(path){const r=await fetch(API+path);if(!r.ok)throw Error(await r.text());return r.json()}
function showPage(p){if(!titles[p])p='dashboard';document.querySelectorAll('.page').forEach(x=>x.classList.toggle('active',x.dataset.section===p));document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x.dataset.page===p));$('#page-title').textContent=titles[p];location.hash=p;if(p==='models')renderModels();if(p==='benchmarks')renderBenchmarks();if(p==='execute')renderExecute();if(p==='results')renderResults();if(p==='ranking')renderRanking()}document.addEventListener('click',e=>{const b=e.target.closest('[data-page]');if(b)showPage(b.dataset.page)});window.addEventListener('hashchange',()=>showPage(location.hash.slice(1)));
async function boot(){try{[categories,models,questions]=await Promise.all([get('/categories'),get('/models'),get('/questions')]);$('#status').textContent='● API connectada';renderCategories();renderModels();renderBenchmarks();renderExecute();await renderStats()}catch(e){$('#status').textContent='○ Backend no connectat';renderCategories()}}
function renderCategories(){$('#categories').innerHTML=categories.length?categories.map(c=>`<button class="category-card" data-page="execute"><strong>${escapeHtml(c.name)}</strong><span>${c.question_count} preguntes</span></button>`).join(''):'<div class="empty">No hi ha categories disponibles.</div>'}
function renderModels(){$('#models-list').innerHTML=models.length?models.map(m=>`<div class="list-item"><b>${escapeHtml(m.name)}</b><span>${escapeHtml(m.provider)} · ${escapeHtml(m.model_id)}${m.context?' · '+m.context+' tokens':''}${m.active?'':' · Inactiu'} <button class="mini" data-delete-model="${m.id}">Eliminar</button></span></div>`).join(''):'<div class="empty">No hi ha models gratuïts detectats.</div>'}
function renderBenchmarks(){$('#benchmark-list').innerHTML=`<div class="list-item"><b>AIBeigmer Core Benchmark</b><span>${categories.length} categories · ${questions.length} preguntes actives</span></div>`}function renderExecute(){const ms=$('#exec-model');if(!ms)return;const active=models.filter(m=>m.active!==false);ms.innerHTML=active.length?active.map(m=>`<option value="${m.id}">${escapeHtml(m.name)} — ${escapeHtml(m.provider)}</option>`).join(''):'<option value="">Cap model configurat</option>';$('#benchmark-progress').innerHTML=`<b>Benchmark complet preparat</b><p>${questions.length?questions.length+' preguntes actives.':'No hi ha preguntes actives.'}</p>`}function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
async function addModel(){const provider=prompt('Proveïdor: OpenAI, Google, Anthropic, DeepSeek, OpenRouter, Groq o FreeLLMAPI','Groq');if(!provider)return;const canonical=provider.trim().toLowerCase();const api_key=prompt(`API key de ${provider} (només s'envia al backend)`);if(!api_key)return;try{let endpoint,body;if(canonical==='freellmapi'){endpoint='/providers/freellmapi/discover';body={api_key,base_url:prompt('URL base de FreeLLMAPI','http://localhost:3001/v1')}}else{endpoint='/providers/discover';body={provider:provider.trim(),api_key}}const r=await fetch(API+endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw Error(await r.text());const data=await r.json();models=await get('/models');renderModels();renderExecute();alert(`S'han detectat ${data.detected??data.total_detected??0} models del proveïdor i s'han guardat ${data.added??0} models gratuïts per separat.`)}catch(e){alert(e.message||'No s’han pogut detectar els models gratuïts.')}}$('#add-model')?.addEventListener('click',addModel);document.addEventListener('click',async e=>{const b=e.target.closest('[data-delete-model]');if(!b)return;if(!confirm('Eliminar aquest model?'))return;const r=await fetch(API+'/models/'+b.dataset.deleteModel,{method:'DELETE'});if(!r.ok)return alert(await r.text());models=await get('/models');renderModels();renderExecute()});showPage(location.hash.slice(1)||'dashboard');boot();